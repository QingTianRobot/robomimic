# Make robomimic workflow commands available to interactive and non-interactive Zsh.
if [[ -r /usr/local/share/robomimic/robomimic-functions.zsh ]]; then
  source /usr/local/share/robomimic/robomimic-functions.zsh
fi
